FILE(GLOB_RECURSE PTHREAD_SOURCES *.c)
FILE(GLOB_RECURSE PTHREAD_HEADERS *.h)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/include)

ADD_LIBRARY(Pthread ${PTHREAD_SOURCES} ${PTHREAD_HEADERS})
SET_TARGET_PROPERTIES(Pthread PROPERTIES LINKER_LANGUAGE C)

